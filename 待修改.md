
## 需求总结

### 1. 弹窗滚动锁定（Scroll Lock）
**问题**：打开子弹窗后，底层页面仍然可以滚动，导致体验混乱。

**期望行为**：
- 打开任何弹窗时，**底层页面禁止滚动**（`body overflow: hidden`）
- 关闭弹窗时恢复底层页面滚动
- 多级弹窗嵌套时（如订阅弹窗内再开文档选择弹窗），始终只锁一次、关闭最外层时统一解锁即可

### 2. 弹窗可拖拽移动
**问题**：弹窗固定在屏幕中央，无法拖动调整位置。

**期望行为**：
- 弹窗标题栏（`.sub-head` / `.doc-dialog .sub-head`）支持鼠标拖拽移动整个弹窗
- 拖拽时弹窗跟随鼠标移动，松手后停在当前位置
- 不超出浏览器可视区域边界（可选，但建议加上）
- 触摸设备支持 touch 事件拖拽（可选）

---

**涉及文件**：`publish/theme.css`（CSS）、`publish/js/ui/subscribe-dialog.js`（JS 逻辑）

---
好的，我来总结成提示词，你看看对不对：

---

## 需求总结

### 1. 评委工号搜索框实时过滤
**问题**：当前在评委角色下拉中搜索时，输入关键字后不立即显示匹配结果，只显示"共 X 项"总数，需要点击才能展开选择。

**期望行为**：
- 在可搜索下拉中输入关键字时，**立即过滤并展示匹配选项**（和调用方系统、批次等搜索框行为一致）
- 不需要额外点击才能看到搜索结果


### 3. 评委部门取 teamName 字段
**问题**：评委表格中部门列应该显示 `teamName` 字段的值，如 `中国银行软件中心（深圳）开发三部`

**期望行为**：
- 评委表格的部门列从接口返回的 `teamName` 字段读取并展示
- 而不是其他字段（如 `deptName` 或空值）

---

**涉及文件推测）：`publish/js/ui/subscribe-dialog.js` 中的评委数据渲染逻辑

好的，我来总结成提示词，你看看对不对：

---

## 需求总结

### 1. 文档筛选支持按批次选择
**问题**：当前文档筛选的"文档批次"下拉可能没有正确联动或无法先选批次再查文档。

**期望行为**：
- 打开文档选择子弹窗后，先在"文档批次"下拉中选择目标批次（如 2609批次、26年10月独立等）
- 选择批次后，文档列表自动过滤为该批次的文档
- 批次筛选应该和"文档编号"搜索一样实时生效（不需要额外点查询按钮）

### 2. 关联文档表格优先显示已选/可勾选的文档
**问题**：文档列表中所有文档混在一起排序，用户不容易找到可以选的文档。

**期望行为**：
- 在文档表格中，**已被勾选的文档排在最前面**
- 或者：**非成员文档（isMember="0"，不可选）排到最后面**
- 总之让可操作/已选中的文档视觉上更突出，不用翻到底部去找

---

**涉及文件推测）：`publish/js/ui/subscribe-dialog.js` 中的文档筛选和排序逻辑

---
