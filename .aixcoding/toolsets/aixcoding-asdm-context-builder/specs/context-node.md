> **⚠️ 模板声明**
>
> - 本文件是规范模板（Spec Template），非最终内容。
> - 适用对象：`.asdm/contexts/index.md` 中"结构型上下文导航"表列出的每一个业务边界节点。
> - 执行 `/asdm-context-init` 步骤 3.2 时，AI 按此模板在对应源码目录下生成 `CONTEXT.md`（与源码同目录，不集中存放）。
> - 若该节点同时是"分层架构约定"的定义节点（即其直接子目录为技术分层目录，且是同构 module 中首次出现），额外包含"分层架构约定"一节；否则该节省略或写"遵循 {上级路径}/CONTEXT.md"。

---

# [节点名称] — 结构上下文

## 职责

[该节点做什么，一到两句话]。不负责 [明确排除的职责]（见 [相关节点路径]）。

## 基准路径

> 本节点对应的真实相对路径（从仓库根目录算起）。所有后续路径模板均以此为基准拼接，避免每次都要现场 `list_dir` 才能定位。

```
{基准路径，如 backend/module1/src/main/java/com/isoftstone/order}
```

## 目录树（真实结构，非抽象模板）

> 由生成时的 `list_dir` 结果直接摘录，不做抽象改写。仅展开到本节点职责范围内有意义的层级（通常 1~2 层），技术分层目录展开到目录名即可，不列具体文件（文件级由下方"分层架构约定"的路径模板覆盖）。超过 15~20 个条目时按子域分组折叠。

```
{基准路径}/
├── controller/
├── service/
├── entity/
├── mapper/
├── ...
```

## 关键文件 / 子模块

> 仅列入口文件和高频改动点，不逐文件罗列。若子节点数量 > 15~20 个，按业务域分组列出。**必须给出相对仓库根目录的真实路径，不能只写文件名。**

| 文件/子目录 | 相对路径 | 说明 |
|---|---|---|
| [文件或子目录名] | `{基准路径}/[实际相对路径]` | [一句话职责] |
| ... | ... | ... |

## 对外接口

[该节点对外暴露的 API / 导出模块 / 公共方法概览，详细文档链接见主题型 L2 的 api.md]

## 依赖关系

| 依赖对象 | 说明 |
|---|---|
| [其他节点路径] | [为什么依赖，通过什么方式调用] |

## 分层架构约定

> 仅当本节点直接包含技术分层子目录（如 controller/service/entity/mapper/adapter/config/util 等模式化结构）时填写本节。
> 若本节点下所有同构 module 已在某处定义过约定，此处写：「遵循 `[上级/首次定义节点路径]/CONTEXT.md` 的统一分层架构约定，无特殊差异。」不重复书写完整表格。

| 包/目录 | 职责 | 命名约定 | 路径模板（基准路径 + 目录 + 命名约定，可直接拼出真实文件路径） | 是否对外暴露 |
|---|---|---|---|---|
| [controller] | [接收请求，参数校验，调用 service] | [XxxController] | `{基准路径}/controller/{Entity}Controller.java` | 是 |
| [service] | [核心业务逻辑] | [XxxService] | `{基准路径}/service/{Entity}Service.java` | 否 |
| [entity] | [数据库实体映射] | [Xxx] | `{基准路径}/entity/{Entity}.java` | 否 |
| ... | ... | ... | ... | ... |

**Agent 使用规则**：任务涉及具体业务实体 `{Entity}`（如 `Order`）时，将上表"路径模板"中的 `{Entity}` 替换为实际实体名，直接拼出完整真实路径（如 `backend/module1/.../controller/OrderController.java`），无需现场 `list_dir` 或 `search_file` 探索。若拼出的路径不存在，才回退为搜索。

## 子节点索引

> 若本节点下还有其他 `[边界]` 子目录，在此列出，形成向下的导航链路（对应 index.md 结构型导航表的同一体系）。

| 子节点 | 路径 | 说明 |
|---|---|---|
| [子模块名] | [相对路径/CONTEXT.md] | [一句话职责] |

---

*由 ASDM 上下文构建工具集维护。最后更新与仓库实际结构保持一致，若目录发生分类漂移（如新增业务分叉子目录），需重新执行 `/asdm-context-update` 校验。*
