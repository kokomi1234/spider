# 2026-09-07 工作记录

> 合并说明：这天上午的记录原本在 `.opencode/memory/`，下午的在 `.workbuddy-ai/memory/`。
> 2026-09-08 整理两个记忆目录时并到一处，同时按用户要求删除了 09-04 之前的日志。

## 上午：字号放大 + 接口预留层（原 .opencode）

## 承接 09-06 未完成项（用户「继续」）

### 需求
1. 用户 09-06 17:09：「字体感觉有点小了」→ 全局放大字号。
2. 「详情和订阅后续是有接口接入的，可以预留一下」→ 为详情 / 订阅预留接口接入层（按项目约定**不写真实接口代码**，只铺占位适配层 + 本地兜底）。

### 改动
- `publish/theme.css`
  - `:root` 新增 `--fs-*` 字号令牌梯度：nano 12 / xs 13 / sm 13.5 / md 14 / base 15 / lg 16 / xl 20 / 2xl 24。
  - 把 34 处 `font-size: Npx` 迁到 `var(--fs-*)`：body 14→15(base)、`.stat .val` 22→24(2xl)、页面 h1 22→24(2xl)。
  - 控件高度随字号放大：`.btn-sm` 36px / `.btn-xs` 30px / 输入框 42px / select 行高 42px。
- `publish/index.html`
  - 12 处 inline `font-size` 迁令牌；表格 th/td 内边距 `9px 8px`→`10px 9px`。
  - 列宽微调防止「是否已核对」表头换行：`--c-check` 82px、`--c-dept` 窄屏 134px。
  - 引入 `<script src="service-api.js">`（在 api-client.js 与 batch-data.js 之间）。
  - 详情弹窗加 `#detailStatus` 状态行（加载中 / 错误），配 `.detail-status` 样式（spinner + is-error 红底）。
- `publish/service-api.js`（新文件，预留接口层）
  - 读 `window.__APP_CONFIG__.endpoints`（默认全空），导出 `window.ServiceApi = { endpoints, isEnabled, fetchServiceDetail, subscribe, unsubscribe, fetchSubscribeList }`。
  - 关键：`isEnabled(name)` 为 false（endpoint 未配）→ 直接返回 `{ok:true, local:true}`，**不发起任何网络请求**；配置后才走 `API.call`。所有方法不抛异常。
  - `buildDetailBody(row)` 带候选 key（serviceId/serverCoding/serviceNumber/sysServeNo/prodBatch）+ `TODO(抓包后)`；`adaptDetail` 兼容 `{data}|{body}|plain|array`；`subscribe` body 暂为 `{serverCoding}`。
- `publish/index.js`
  - `viewDetail` → `async viewDetail(row)`：加 `detailSeq` 守卫（丢弃过期异步响应），先渲染本地 32 字段，再 `await ServiceApi.fetchServiceDetail` 合并远端；失败显示 is-error「详情接口暂不可用，已显示列表中已有的字段」。
  - `quickSubscribe(serverCoding, btnEl)` → `async`：内部先做 `isSubscribed` 去重，禁用按钮「订阅中…」，成功后写入 SubscribeManager + 应用筛选 + 更新统计。
- `publish/subscribe-ui.js`
  - 删除 / 新增订阅改为 `async`，先 `await ServiceApi.unsubscribe/subscribe`，失败回滚按钮并 toast，成功再更新。
- `publish/runtime-config.js`
  - 默认加 `endpoints:{}` / `endpointMethods:{}`，并注释真实路径示例（如 `serviceDetail:'/itamp-tool/publish/getServiceDetail'`）+ 抓包后补全 `build*/adapt*` 的说明。

### 验证（零依赖 CDP，`/tmp/verify-font-api.js`）
全部通过：
- 字号令牌生效：body 15px、stat.val 24px、输入框 42px、h1 24px。
- 布局未崩：无横向溢出；筛选卡与结果卡左右对齐（Δl=0,Δr=0）；表格无横向滚动；渲染 50 行。
- 预留层空配置：详情状态行隐藏、渲染 32 本地字段；表内订阅走本地成功（toast「已订阅」）；**reserved 请求数 = 0**（确认未配 endpoint 时零网络请求）。
- 预留层配假 502 接口：`is-loading` 出现 → 失败回退本地 + 错误横幅，仍渲染 32 字段，无白屏；确认确实向 `getServiceDetail` 发了请求。

### 清理
- 停掉后台 Chrome(9222) 与静态 server(8090)。
- 临时脚本：`/tmp/verify-font-api.js`。

## 结论
两项需求均完成并验证：全局字号已放大且可一处调节；详情/订阅已预留干净的接口适配层，未配置时纯本地、零请求，抓包后只需补 `endpoints` 与 `build*/adapt*`。

## 下午：订阅弹窗重建（原 .workbuddy-ai）

## 补完：订阅弹窗（参照 `/Users/a1/Desktop/文本.docx`）

### 现象
用户反馈「可能运行不起来」，先看隐藏目录：
- `.aicoding` / `.workbuddy` 不存在；实际是 `.aixcoding`（harness 工具集配置）和 `.opencode`（项目长期约定 + 日志）
- `.opencode/memory/MEMORY.md` 是项目铁律（没有抓包就不要写接口代码）
- `.opencode/memory/2026-09-07.md` 记录的是早上的字号放大 + 接口预留层，没提弹窗

### 已修的硬阻塞
1. **`publish/index.js:40`** 末尾多了一个 `·`，整个文件无法解析 → 整页死。删字符。
2. **`publish/subscribe-dialog.js`** 是上一次未写完就中断的文件（233 行处 `/** 关闭订阅弹窗` 戛然而止），且未在 `index.html` 的 `<script>` 列表中加载。完全重写为单文件模块。
3. **`publish/index.js` 里 1265..1703 区段**是上一版内联写的弹窗逻辑，和新的 `subscribe-dialog.js` 完全重复 → 整段删除，改为 `window.SubscribeDialog.open(row)` 调用；新增 `window._afterSubscribeChanged` 钩子供弹窗在确认后回调刷新。
4. **`publish/service-api.js`** 缺 `subscribeWithForm`，确认时调用会抛 `TypeError` → 补一个未配 endpoint 就走本地的预留版本，body 透传 `form`，等抓到订阅接口真实报文再补字段映射。
5. **`publish/theme.css` 完全没写 `.sub-*` / `.doc-*` / `.judge-*`** → 追加 ~190 行样式（含 sticky 表头、自适应两栏 grid、表格 fixed-height 滚动、文档筛选 + 分页栏）。

### 按文档重建的 HTML（`publish/index.html` 859..1015）
- 基础信息区 25 字段（按文档第 1 节），含：调用方系统 / 关联文档 / 5 个带输入下拉 / 7 个标准下拉 / 8 个文本框
- 性能指标区 6 字段（日常 + 峰值，峰值 TPS 标必填）
- 订阅备注 textarea
- 评委信息表格 6 列：复选框 / 序号 / 评委角色 / 评委工号 / 评委姓名 / 评委部门
- 关联文档子弹窗：文档编号 + 文档投产批次 筛选，文档编号 / 文档名称 / 文档批次 表格，分页（10/30/50、首页、上一页/下一页、跳页）

### 按「没有抓包就不要写接口代码」铁律的兜底
- 调用方系统：复用 `window.loadProviderList()`（与提供方系统共用一次请求，零额外开销）
- 调用方投产/变更批次：复用 `window._batchOptions`（来自抓包中 `conditions/subscribe` 的 `batchList`，共 295 条）
- 服务方式 / 接口方式 / 消息中间件 / 消息通信类型 / 评委角色 / 调用方应用系统服务编号 / 网关服务编码 / 分组CONTEXT / MCIS交易码 / IPS交易码 / 评委工号：抓包没有这些字典，`subscribe-dialog.js` 顶部集中放在 `DICT` 对象里并标 `TODO(抓包后)`；下拉缺数据时显示 `（待抓包补全）`，让 UI 不崩，等拿到订阅表单的字典接口后只需改一处
- 关联文档列表：没有抓包 → 走本地兜底数据 68 条，前端分页 + 筛选；接上真实接口只需替换 `getDocRows()`

### 验证（零依赖 CDP，Headless Chrome + 本地 node http server）
脚本：`/tmp/verify-sub.js` / `/tmp/shot-sub.js`
- 全 7 个步骤绿：弹窗打开 / 布局 32 行 / 文档子弹窗打开且选行回填 / 评委加行 / 手输调用方系统（捕获阶段监听保活） / 校验拦截空 TPS / 确认订阅写入 `SubscribeManager`
- 带 `PROXY_OFFLINE=1 node proxy.js` 时：`window._batchOptions.length === 295`，3000 端口离线缓存正常喂数据
- 视觉确认：两栏 grid 在 ~1100px 对话框宽度下正常对齐，性能/评委/备注在滚动区下半部

### 产物文件
- 改：`publish/index.html` / `publish/index.js` / `publish/theme.css` / `publish/service-api.js`
- 写：`publish/subscribe-dialog.js`（~470 行）
- 图：`output/订阅弹窗预览.png` / `output/关联文档子弹窗.png` / `output/订阅弹窗-填写示例.png`
## 收尾：风格一致性审计

### 发现的根因
- `publish/index.html` 的 `<style>` 块里残留着上一版弹窗的**整段旧 CSS**（~340 行），包括 `#subscribeOverlay { z-index: 1100 }`、`#subscribeDialog { max-width: 860px }` 这些 **ID 选择器**。它们的优先级 (0,1,0,0) 高于我新写的 `.sub-dialog` 类 (0,0,1,0)，把 theme.css 里的 `max-width: 1080px` 完全压住，弹窗一直停在 860 / 95% 上限。
- 顺手把所有旧 `#xxx { ... }` 删了，并写了一段注释说明弹窗样式一律去 theme.css。

### 按 :root 令牌逐项对齐
- `--fg` 是**不存在的**（项目里是 `--on-surface`），全替换掉。
- 控件：38px → **42px**、padding `0 10px` → `0 12px`、`--fs-sm` → **`--fs-md`**、placeholder 用 `--faint`、disabled 用 `--faint`（不是 `--muted`，跟既有 `.form-group` 一致）。
- 标准 `<select>` 加上 `appearance:none` + 同款自定义箭头 SVG + `padding-right: 34px`（跟 `.form-group select` 一致）。
- 表格：font-size `--fs-sm`、th 用 `--fs-nano / 600 / --muted / letter-spacing:.02em / padding: 10px 9px / border-bottom: var(--outline-soft)`、td `padding: 10px 9px`、加 `line-height:1.4`（跟 `.result-table` 一致）。
- 表内控件 34px、`accent-color: var(--primary)`、16×16 复选框。
- 分页 `select / jump input` 用 btn-xs 同款 30px / `--fs-nano`。
- 间距、圆角全部走 `--sp*` / `--r-*` 令牌。
- z-index 文档化：页面 < `.overlay` 1000 < 子弹窗 1050 < loading-mask 2000 < toast 3000。

### 提示文案
- 7 个可搜索下拉 placeholder `输入关键字搜索 / 选择` → `请输入或选择X`（对齐页面筛选区）。
- 评委行 `输入姓名或EHR号搜索` → `请输入姓名或EHR号`、`自动带出 / 可手填` → `请输入或选择`。
- 按钮文案「取 消 / 确 认 / 选 择 / 查 询 / 重 置」带空格——和页面主按钮「🔍 查 询 / ↺ 重 置」风格一致，**不需要改**。

### 程序化比对（`/tmp/style-audit.js`）
把弹窗和页面对应控件的 `getComputedStyle` 逐项对照，结果：
- 文本输入框 ✅ / 可搜索下拉 ✅ / 表单标签 ✅ / 表头 ✅ / 单元格 ✅，5/5 全部一致。
- 额外体检：dialog 1080 / doc 820 / 无横向溢出 / grid 2 列 / z-index 1000/1050 / 必填星号渲染。
- 功能回归 verify 全绿（修正了 verify 脚本里残留 localStorage 导致的 false negative）。


## 项目深度审查（仅分析，不删除）

- 项目是原生 HTML/CSS/JS，无 package.json / lockfile / requirements / CI；不存在可验证的“废弃依赖”清单。
- `publish/index.js` 1600 行承担查询编排、分页/重试、渲染、导出、详情、事件绑定、初始化和跨模块桥接，是当前最大上帝文件。
- 全局依赖通过 `window.*` 字符串桥接：`index.js`、`subscribe-dialog.js`、`subscribe-ui.js`、`service-api.js` 间存在隐式依赖和初始化顺序约束。
- 明确可人工确认清理：`publish/date-picker-old.js`、`publish/date-picker.js.backup2`（两者与当前 `date-picker.js` MD5 相同/无运行时引用）；`publish/searchable-select.js.bak`（无运行时引用）；`publish/test-batch.html`（独立旧测试页，无入口引用）。
- `publish/mock-data.js` 未被 HTML 引用但 README 明确标为调试用，属于保留或移到 `tools/` 的人工确认项；`publish/style.md` 被 README 引用，不能直接删，建议迁移为 docs/design-system.md。
- `publish/cache/` 9 个文件含真实内网缓存，README 明确提示勿外传/提交；应移出版本管理或加入 ignore，但未获用户确认不删除。
- `publish/.env` 权限为 0644 且含 PROXY_TOKEN；HAR 三份抓包记录均含 token/Cookie/ssopSessionId 请求头，均是高风险敏感资产，建议本地隔离和脱敏副本。
- 根目录没有 `.git`，无法确认提交追踪、分支和现有 ignore 规则；建议先初始化版本控制并添加 `.gitignore`，但不要改变用户目录。


## 21:08 继续执行审查建议（内网资产保持原样）

- 将 `publish/date-picker-old.js`、`date-picker.js.backup2`、`searchable-select.js.bak`、`test-batch.html` 移入 `publish/tools/legacy/` 隔离，未删除。
- 将 `mock-data.js` 移入 `publish/tools/mock-data.js`，将 `style.md` 移入 `publish/docs/design-system.md`，并更新 README 结构树。
- 新增根目录 `.gitignore`：忽略 `.DS_Store`、真实 `.env`、`publish/cache/`、HAR、日志和预览 PNG；未删除任何高风险资产。
- 新增 `publish/.env.example` 作为无凭据配置模板，README 改为引用该模板。
- 后续待处理：拆分 `index.js` 与集中启动依赖；当前先完成资产归位和规则补齐，确保页面引用保持不变。


## 21:08-21:10 执行全部结构整改（高风险内网资产未触碰）

- 隔离旧文件到 `publish/tools/legacy/`：`date-picker-old.js`、`date-picker.js.backup2`、`searchable-select.js.bak`、`test-batch.html`；没有删除。
- 归位 `publish/tools/mock-data.js` 与 `publish/docs/design-system.md`，更新 `publish/README.md` 结构说明。
- 新增根 `.gitignore` 与 `publish/.env.example`；忽略真实 `.env`、HAR、`publish/cache/`、`.DS_Store` 和预览 PNG。实际 `.env`、HAR、cache 均保持不变。
- 新增 `publish/bootstrap.js`：检查 API / SubscribeManager / searchable-select / CsvExporter 依赖，并建立 `AppRuntime`、`AppServices` 注册表。
- 新增 `publish/csv-export.js`，把 CSV 序列化和下载从 `index.js` 拆出；`index.js` 保留薄编排入口。
- `index.js`、`subscribe-dialog.js`、`subscribe-ui.js` 的 toast/订阅刷新改为优先走 `AppServices`，保留旧 `window._showToast` / `_afterSubscribeChanged` 兼容回退。
- 语法检查通过；订阅弹窗回归通过：32 行表单、文档选择、评委新增、必填校验和本地订阅确认均正常。离线测试中的网络错误仅来自真实接口不可达，符合当前测试环境。


## 23:17 变更时间日期选择器修复

- 用户反馈：打开后输入框中间有高亮线、面板宽度与输入框不一致、日期点击不生效。
- 根因：旧 `date-picker.js` 克隆了第二个 input 并对 wrapper/input/panel 大量内联样式；面板虽写 `left:0/right:0`，但 `min-width:280px` 造成宽度撑开；日期按钮用 `var date` 闭包，点击时可能取到循环末值；外层 click 与内部按钮 click 冒泡导致面板状态再次切换。
- 修复：复用原始 `#f_changeTime` input；统一 `.dp-*` 样式到 `theme.css` 令牌；面板 `width:100%`；日期循环用 `let`；阻止面板按钮事件冒泡；保留今天/清除/ESC/键盘交互。
- 验证：`/tmp/verify-date.js` 通过；面板与 wrapper 宽度差 0px、点击日期回填 `2026-09-01` 并自动关闭、清除恢复空值、运行时异常 0。验证报告：`output/变更时间修复验证.md`。


## 23:39 日期选择器年月视图修正

- 顶部标题点击逻辑改为：日期视图 → 第一次点击进入月份选择（12 个月）→ 再点击标题进入年份选择（12 年）→ 选择年份回到月份 → 选择月份回到日期。
- 左右箭头随视图变化：日期视图按月、月份视图按年、年份视图按十年。
- 样式复用已有 `.dp-month-grid` / `.dp-year-grid` / `.dp-month-cell` / `.dp-year-cell` 令牌。
- 自动化验证通过：三种视图数量分别为 30 天 / 12 月 / 12 年，无运行时异常；选择年份和月份能正确回到下一层。
