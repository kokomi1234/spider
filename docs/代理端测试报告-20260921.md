# 代理端测试报告（2026-09-21）

> 测试对象：`publish/proxy.js`（含 `publish/lib/queries-db.js`、本地数据端点、静态服务、转发回放）
> 代码基线：`47c4d49` @ `workbuddy/dev-e2d2ccd2`
> 测试方式：起两个真实代理实例 + HTTP 直打（非浏览器），共 **约 65 项**
> 结论：**未修改任何代码**，只列问题与建议。
>
> ⚠️ **本文有 8 处结论已被后续 63 个提交甩过时**（主要是 `50472ac`/`f416ba4`/`f4e939c`/`d073d5b`
> 把 token 改成"只存浏览器本机、代理不保存任何用户凭证"，以及 `d073d5b` 删掉启动横幅里的录制/缓存那几行）。
> **以文末「§六、2026-09-22 结论更正」为准**；正文里过时最严重的三行已就地打了 ⚠️ 标记。
> 另：本轮复测新查出并已修掉的三条（超时撞车、`PROXY_TIMEOUT` 不热更、未知本地路径落到转发层）见
> `docs/前端与代理复测报告-20260922.md`。

---

## 一、测试实例与隔离

| 实例 | 端口 | 模式 | 缓存目录 | 数据目录 |
|---|---|---|---|---|
| A | 3056 | `PROXY_OFFLINE=1` 离线回放 | 真实录制（82 条） | 临时 `q3056.db` / `bt3056.json` |
| B | 3057 | 非离线（内网不可达） | 空临时目录 | 临时 `q3057.db` / `bt3057.json` |

`.env` 通过 `PROXY_ENV_PATH` 指向临时文件，**未触碰项目根 `.env`**；
常用查询库指向临时 SQLite，**未触碰 `shared/saved-queries.db`**；
`/cache/clear` 只对实例 B（空缓存）执行，**未清空真实录制**。

---

## 二、被测项与结果

### 2.1 健康检查与路由（6 项，全通过）

| 被测项 | 结果 | 实测值 |
|---|---|---|
| `GET /health` | ✅ | `code:200 offline:true cacheCount:82 target:http://itamp.bocsys.cn` |
| `/health` 如实播报 cacheDir | ✅ | `C:/Users/10669/Desktop/spider/publish/cache` |
| `GET /` → 302 | ✅ | `Location: /home` |
| `/home` `/publish` `/task` `/subscription` | ✅ | 均 200 + HTML |
| 启动横幅播报绑定/鉴权/录制/超时 | ⚠️ **已过时** | 见 §六 第 1 条（录制/缓存那几行已删，改成播报「本次用谁的 token」） |
| `.env` 热更新（`/reload`） | ✅ | `{"code":200,"msg":"reloaded","keys":[]}` |

### 2.2 静态文件服务（9 项，全通过）

| 被测项 | 结果 | 实测值 |
|---|---|---|
| HTML / CSS / JS 的 Content-Type | ✅ | `text/html`、`text/css`、`javascript` 均正确 |
| ETag → 304 | ✅ | 带 `If-None-Match` 返回 304 |
| Cache-Control | ✅ | `no-store`（默认 `PROXY_STATIC_MAX_AGE=0`） |
| 不存在的文件 → 404 | ✅ | |
| 访问目录 → 403/404 | ✅ | |
| 目录穿越 `/..%2f..%2f..%2fWindows%2fwin.ini` | ✅ 被拦 | 404 |
| 目录穿越 `/%2e%2e/%2e%2e/...` | ✅ 被拦 | 404 |
| 绝对路径 `/C:/Windows/win.ini` | ✅ 未泄漏 | 404 |
| `HEAD /home`、`HEAD /local/batch-times` | ✅ | 200 且无 body |

### 2.3 管理端点（7 项，全通过）

| 被测项 | 结果 | 备注 |
|---|---|---|
| `GET /cache/list` | ✅ | `count:82` |
| `GET /cache/clear` | ✅ | 「已清空 N 个缓存文件」 |
| `GET /reload` 不泄露值 | ✅ | 只回键名，不回 token/target 值 |
| `GET /admin/token/status` | ⚠️ **字段已变** | 见 §六 第 2、3 条：预览其实是**前 8 + 后 4**，且多了 `adminUserId`/`expiryHour` |
| `POST /admin/token` | ⚠️ **语义已变** | 见 §六 第 2 条：现在**只改管理员那个全局 token**，不再管用户 token |
| 超 1KB token → 413 | ✅ | |
| 非法 JSON → 400 | ✅ | |

### 2.4 `/local/batch-times`（9 项，全通过）

| 被测项 | 结果 | 备注 |
|---|---|---|
| GET 空配置兜底 | ✅ | `{code:200,data:{batchTimes:{}}}` |
| **按键合并**（甲改 2609、乙改 2611） | ✅ | 两条都在，**不互相覆盖** |
| 同名键覆盖 | ✅ | `2609` 被新值覆盖 |
| PUT 可用 | ✅ | |
| DELETE → 405 | ✅ | |
| 超 64KB → 413 | ✅ | |
| 非法 JSON → 400 且不写坏文件 | ✅ | 之后 GET 仍正常 |
| 原子写无 `.tmp` 残留 | ✅ | |
| 落到 `PROXY_BATCH_TIMES_FILE` 指定位置 | ✅ | `bt3057.json` |

### 2.5 `/local/saved-queries`（11 项，全通过）

| 被测项 | 结果 | 备注 |
|---|---|---|
| GET（SQLite 分支） | ✅ | `storage:"sqlite"` |
| 写入 + 读回 | ✅ | `people:2` |
| `?user=88001` | ✅ | 只回该人的，`mode:"user"` |
| `?dept=K4229` | ✅ | `mode:"dept"`，回 2 条 |
| `?dept=` 不存在部门 | ✅ | 空列表且 **mode 仍是 dept**（不降级成全量） |
| 删除 → items 移除 + 立墓碑 | ✅ | `deleted:[{key:"sq-a"}]` |
| **墓碑防复活**（旧副本再推不复活） | ✅ | |
| 重复 upsert 幂等 | ✅ | 不增条 |
| body 直接给数组 | ✅ | |
| 缺 `items` → 400 | ✅ | `body 需要 { items: [...] }` |
| 超 2MB → 413 | ✅ | |

### 2.6 离线回放与转发（6 项，全通过）

| 被测项 | 结果 | 实测值 |
|---|---|---|
| 离线精确命中 | ✅ | `X-Cache-Match: exact`、`X-Served-From: cache` |
| 离线宽松匹配 | ✅ | `X-Cache-Match: loose` |
| 离线未命中 → 404 | ✅ | 「离线模式：本地缓存中没有这条记录…」 |
| 内网不可达 → 502 | ✅ | 126ms 返回，`msg` 含 `getaddrinfo ENOTFOUND` |
| 502 也带 CORS 头 | ✅ | `access-control-allow-origin: *` |
| `OPTIONS` 预检 | ✅ | 204 + `Access-Control-Allow-Headers: Content-Type, token` |

### 2.7 并发与健壮性（7 项，全通过）

| 被测项 | 结果 | 实测值 |
|---|---|---|
| 30 并发 `/health` | ✅ | 30/30，21ms |
| 混合并发（静态+本地+回放） | ✅ | 无异常 |
| **20 并发写 saved-queries** | ✅ | **0 丢失**（20/20），`people:9` |
| 畸形 body / 空 body | ✅ | 不崩，有响应 |
| 4000 字符超长 URL | ✅ | 404，不崩 |
| `/local/*` 未知路径不落到转发 | ⚠️ **结论不变、实现已换** | 见 §六 第 4 条：当时靠"恰好没匹配上"，2026-09-22 起由 `handle()` 入口统一 404（`/health?t=1` 也一并修好） |
| 跨实例数据隔离 | ✅ | 两个实例 batchTimes 互不影响 |

### 2.8 落盘与持久化（4 项，3 通过 / 1 风险）

| 被测项 | 结果 | 备注 |
|---|---|---|
| SQLite 文件落盘 | ✅ | `q3057.db` |
| WAL 模式 | ✅ | `q3057.db-wal` 存在 |
| 原子写无 `.tmp` 残留 | ✅ | |
| **只拷 `.db` 能否独立打开** | ❌ **失败** | 见问题 **P1** |

---

## 三之一、2026-09-21 13:2x 修复状态（拉取 `aa0edbe` 后）

| 编号 | 问题 | 状态 | 说明 |
|---|---|---|---|
| P1 | 只拷 `.db` 丢全部数据（WAL） | ✅ **已修复（文档）** | `shared/README.md` 新增「备份与恢复」节（含 `tools/backup-queries-db.js` 用法与恢复三步）；`publish/README.md` 团队共享节加警告。**代码未动**——备份脚本本来就对，缺的是没人知道它存在 |
| P2 | 超 2000 条截断静默 | ✅ **只修了一半** | `proxy.js:735` 起：算出 `truncated` 并回传，响应 `msg` 也写明丢弃条数。实测提交 2500 → `truncated: 500`、`msg: 已合并保存（超出 2000 条上限，丢弃 500 条）`。**但前端从不读 `truncated`** —— 2026-09-22 复测 D-5 才补上（同时补了库层的 `skipped` 计数：字段不全/没归属人的记录当时也是静默丢） |
| P3 | `/admin/token/status` 回显 `.env` 路径 | ➖ **不改（记录）** | 内网自用、一台部署前提下可接受；token 本身只露前后 5 字符 |

> 附带修正：`shared/README.md` 原有一段指引用户设 `PROXY_ADMIN_TOKEN`，
> 但该开关 **2026-09-20 已整体删除**，属过时文档 —— 已改述为现状（不鉴权是拍板结果，
> 要收窄只能设 `PROXY_HOST=127.0.0.1`）。

---

## 三、发现的问题

### P1 ｜ 只拷贝 `saved-queries.db` 会丢掉全部数据（WAL 未 checkpoint）【中】

- **关联文件**：`publish/lib/queries-db.js`（WAL 模式）、`shared/README.md`、`publish/README.md`
- **实测证据**：
  - `q3057.db` 主库 **4096 字节**，`q3057.db-wal` **1.79 MB** —— 全部数据与**建表语句**都在 WAL 里。
  - `fs.copyFileSync('q3057.db', 'copy-only.db')`（不带 `-wal`/`-shm`）后单独打开：
    **`no such table: saved_queries`** —— 连表都不存在，数据 100% 丢失。
  - 代理进程被杀（`kill`）后，WAL 仍然存在、主库仍未 checkpoint。
- **已有的正确兜底**：`publish/tools/backup-queries-db.js` 用 `VACUUM INTO` 做备份，
  实测产出 **836 KB 自包含文件**，孤立拷贝仍读到 **4023 行 + 1 墓碑 + 10 个保存人**。脚本是对的。
- **真正的缺陷在文档**：`shared/README.md` 与 `publish/README.md` **都没有提到**
  ① WAL 的这个坑 ② 有这个备份脚本。按常识 `cp shared/saved-queries.db backup.db` 就会拿到空壳。
- **修复建议**：
  1. 在 `shared/README.md` 加一节「备份与恢复」，明写：必须连带 `-wal`/`-shm`，
     或**直接用 `node tools/backup-queries-db.js`**；恢复时删掉旧 `-wal`/`-shm`。
  2. 可选：代理关闭时/定时执行 `PRAGMA wal_checkpoint(TRUNCATE)`，让主库尽快自包含
     （注意别影响写入性能，建议只在 idle 时做）。
  3. 可选：加 `/admin/backup` 端点，直接返回 `VACUUM INTO` 出来的单文件。

### P2 ｜ 超 2000 条截断是静默的【中低】

- **关联文件**：`publish/proxy.js:733`（`store.upsert(incoming.slice(0, 2000), delIds)`）
- **实测**：提交 2500 条 → 收到 2000 条，响应字段只有
  `code,msg,data{items,deleted,file,storage,people,mode}`，**没有任何 `truncated`/`limit` 提示**。
- **影响**：前端导入后的 toast 会报偏大的数字；用户以为全存进去了。
- **修复建议**：响应里加 `truncated: <被丢弃的条数>`，前端据此后半句提示。

### P3 ｜ `/admin/token/status` 回显 `.env` 绝对路径【低】

- **关联文件**：`publish/proxy.js:837-908`
- **实测**：返回 `{"hasToken":true,"tokenPreview":"TEST-TOK...-XYZ","envPath":"C:/.../spider/.env"}`。
- **评估**：token 本身只回前后 5 字符，风险有限；但 `envPath` 暴露了部署机的目录结构。
  在「内网自用、一台部署」的前提下可接受，**不建议为此改动**，仅记录。

---

## 四、已确认的既有设计（非缺陷，不改）

以下均为用户此前拍板的选择（`memory.md` §2、`CHANGELOG.md` 有记录），本次实测只是确认现状：

- `/local/*`、`/cache/*`、`/admin/*`、`/reload` **全部不鉴权** + CORS `*`，默认 `HOST=0.0.0.0` 绑全网卡。
  实测：任意来源可调 `/cache/clear` 一键清空全部录制、`/admin/token` 改写 token 并落盘 `.env`、
  `/local/saved-queries` 读光/改写/删空整个团队库。
  **部署提示**：若代理所在机器暴露在办公网，这些都是无门槛的。要收窄只能设 `PROXY_HOST=127.0.0.1`。
- `502` 响应体里带 `target` 与缓存 `key`（便于排障，也属内部信息）。
- 只录 HTTP 200：后端不可达时不会写入缓存（实测确认，缓存条数不变）。

---

## 五、结论

代理端**功能面无缺陷**：路由、静态服务（含目录穿越防护）、管理端点、两个本地数据端点、
离线回放三态、转发失败降级、并发写入不丢、原子写、请求体大小限制，全部按设计工作。

需要处理的只有三件事，按优先级：

1. **P1** —— 文档补「WAL 备份坑 + 备份脚本」，成本最低、后果最重（数据全丢）。
2. **P2** —— 截断加回执字段。
3. **P3** —— 可选，仅记录。

（前端侧问题见 `docs/前端全量测试报告-20260921.md`，两轮合计 14 个问题：前端 11 + 代理 3。）

---

## 六、2026-09-22 结论更正（对照 `9f542f1` 之后的 63 个提交逐条实跑）

> 逐条来源：`docs/前端与代理复测报告-20260922.md`（代理与网络那一路的实测清单），每条都有 curl 原话或 `文件:行号`。
> 正文里过时最严重的三行（§2.1 横幅、§2.3 两个 `/admin/token`、§2.7 未知 `/local`）已就地打 ⚠️ 指向本节。

| # | 本文原来的结论 | 现在的实情 | 从哪个提交变走的 |
|---|---|---|---|
| 1 | §2.1 启动横幅播报「绑定/鉴权/**录制**/超时」 | 录制、缓存目录、缓存列表 URL 三行**已删**；离线模式下连「转发超时」那一行都不打（`proxy.js` 里那句被 `if (!OFFLINE_REFRESHED)` 包着）。改成播报「🔑 本次用谁的 token」 | `d073d5b` |
| 2 | §2.3 `POST /admin/token` 两条用户分支、= 改写用户 token | 该端点**只管管理员的全局 token**；`mine/source/reason/isAdmin` 字段已删。**用户 token 只存在浏览器本机** `localStorage['spider.userToken.v1']`，代理不保存任何用户凭证 | `50472ac` → `f4e939c` |
| 3 | §2.3 / §三 P3 「token 预览只露前后 5 字符」 | 实测是**前 8 + 后 4**（`proxy.js` 的 `tokenPreview`） | 同上 |
| 4 | §2.7 「`/local/*` 未知路径不落到转发 ✅ 404」 | 复测当时**已经不成立**：`/local/unknown-x`、`/admin/unknown-x`、`/cache/unknown-x` 会被原样转发去打内网（非离线时等满超时才 502），带查询串的 `/health?t=1` 同理（根因是 `/health` 用 `req.url ===` 精确匹配，其余用去掉查询串的 `cachePath`）。**2026-09-22 已修**：`/health` 改走 `cachePath`，`handle()` 入口对本地前缀未匹配者直接 404。实测 `/health?t=1` → 200@2ms、三个未知路径 → 404@1ms | 坏于 63 个提交期间；修于本轮 |
| 5 | §2.2 「目录穿越 → 404 被拦」 | 现在是**两种行为**：带 MIME 扩展名的（`.js`/`.html` 等）→ **403 Forbidden**；不带扩展名的（`/../../../etc/hosts`、`/../.env`）→ **落到转发层**（离线时回 404「本地缓存中没有这条记录」）。**没有发生任何文件泄漏**，但"404 被拦"这句描述已经不准 | 待查（本轮只登记，未改） |
| 6 | §2.5 「超 2MB → 413」 | 上限按 **字符** 判（`text.length > 2*1024*1024`），不是字节：实测 1,957,791 字符 / 2,713,791 UTF-8 字节 → **200**；3,607,791 字符才 413。而且是**收完整个 body 才判**，不限流 | 一直如此（本轮量出来） |
| 7 | §三之一 P2 「截断已修复」 | 只修了代理侧，前端 `saved-query.js` 当时只取 `file/storage/people`，**从不读 `truncated`** → 角标照说「已同步」。本轮已修：前端读 `truncated` + 库层新增 `skipped`（字段不全/没归属人的记录原先静默丢），一并写进提示 | 修于本轮 |
| 8 | §四 「`.env` 存 token、`/admin/token` 落盘 `.env`」的用户凭证面 | 用户 token 完全不落后端；`.env` 里只剩**管理员的全局 token** 一份 | `f416ba4` / `f4e939c` |

**本轮之后新增的事实（本文没覆盖，按现在的行为记录）**：

- `/health` 多了 `timeout` 与 `port` 两个字段；`PROXY_TIMEOUT` 从启动期 `const` 改成可被
  `/reload` 与 1.5 秒 `.env` 轮询改到（原先横幅承诺热更新却不生效，实测改 900 → 2.5 秒后如实播报）。
- 前端请求超时 20000ms 改成 **22000ms** —— 与代理同为 20000 时，代理"掐断后回退读本地缓存"
  那条路（实测 20006ms 交付）永远赶不上浏览器 abort。
- 启动时会自检「同一份常用查询库上是否还有另一份代理在跑」，有就打一行
  `⚠️ 另有一份代理正在跑同一份常用查询库：http://127.0.0.1:<端口>（pid …，启动于 …）`。
  实现方式：`<库文件>.proxy-lock` 记 `{pid,port,host,db,startedAt}`，新起的实例拿 **HTTP 探那个端口的 `/health`** 判活
  （不用 `lsof`/`fuser`，部署机是 Windows；陈锁直接覆盖，所以进程被 kill 也不留残留）。
  **只报不拦** —— 用户 2026-09-22 拍板：不在代码里钉死"一个库文件只许一个实例"。
- 多实例并发的实测代价（本轮量过，作为"为什么要报这一行"的依据）：20 个并发 POST →
  状态码 `{"200":11,"400":9}`、最大耗时 **320701ms**、只落进 10/20 条；那 9 个 400 **响应体是空的**
  （来源未定位到行，用户裁定不追）。
