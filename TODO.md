# 📌 待办清单

> 记录还没做、或在等外部条件的事项。做完一条就删掉（git 历史里能查到，不留"已完成"归档）。
> 项目约定与踩坑见 `.workbuddy/memory/MEMORY.md`。

## 🛠 待办（2026-09-19 首页常用查询 / 发布页改造遗留）

> 本轮做完了：首页三页入口、常用查询（保存/回填/重命名/删除）、当前用户（工号查人）、
> 部门排行 TopN、导出导入、**代理共享端点**（照批次时间那套）、发布页服务编号多选、
> 变更时间起止区间；顺带修了「姓名查询返回登录人」的坑（首页 + 评委搜索都加了核对）。
> 同一天第二轮又收了四条：**同步状态角标**（首页能看出连没连上共享库）、
> **表单控件的 aria-label**、**评委结果核对抽成纯函数 + node 单测**、**task 页导出进度与取消**
> —— 结论都留在下面的引用块里（防重复排查），带 `- [ ]` 的才是还没做或做不了的。

- [ ] **真要跨机器共享还得再配一次 `PROXY_QUERIES_DB`**：默认已落在仓库根的
      `shared/saved-queries.db`（Node 不支持内置 `node:sqlite` 时退回
      `shared/saved-queries.json`），**同一个文件路径才算同一个库**——
      同事各自 clone 的是各自的磁盘，光靠项目里的路径并不会自动同步。
      真要「互相看到」必须在根目录 `.env` 里指到一个双方都能访问的文件
      （网络盘 / 同步盘），配法和合并规则都写在 `shared/README.md` 里，照抄即可。
      **注意**：不要把这条理解成「默认已经共享了」。

- [ ] **服务编号多选的请求体字段没在真实后端验证过**：发布页「提供方应用系统服务编号」
      改成多选后，请求体发 `sysServeNoList`（列表）+ `sysServeNo`（首个）。
      `sysServeNoList` 来自抓包契约里的字段名，但**抓包没有「用它做筛选」的样本**，
      所以后端认不认这个筛选条件还没验证。**需要**：在内网真机查一次，
      对比「不选 / 选一个 / 选两个」的结果条数；或给我一次带该条件的抓包。
      不通过就退回只发 `sysServeNo`。

> **发布页「变更时间」已定（2026-09-19）**：后端没有可用字段，只能前端按 `offerEffectiveTime` 过滤，而它在真实样本里只有约 4% 有值（162 行里 7 行；`effectiveTime`/`createTime`/`updateTime`/`batch` 全 null；同一字段在订阅接口 640 行里有 366 行有值）。**用户拍板：保留筛选、不加覆盖率提醒** —— 筛出来少是数据面事实。本轮只把提示口径从「本页」改成「全量」（`emptyHint` / `droppedFilterHint`；条件本来就作用在拉全并去重后的结果上，说「本页」会让人误翻页）。
> **冒烟的无障碍盲区已补（2026-09-19）**：`smoke-browser.js` 在 publish.html 那段弹窗已打开、评委行已新增之后再量一次弹窗内的 `.searchable-select-input` / `.msel-display` / `.judge-name` / `.judge-dept`（实测 18 个全有名），并写死四个名字「评委角色 / 评委工号 / 评委姓名 / 评委部门」—— 弹窗里没有可见 label，角色与工号完全依赖 `subscribe-dialog.js` 传的 `opts.label`，删掉就红（反证做过）。**记一笔**：订阅弹窗在 `publish.html`，不在 subscription.html。
> **共享端点的并发已实测收口（2026-09-19）**：「后写者会覆盖计数」是**旧 JSON 实现**的语义，SQLite 下不成立 —— 单代理并发实测安全（数据见上一节）。代理内部另有两处已修的如实化：JSON 兜底分支以前不发 `mode/storage/people`（前端防线看不见降级），坏文件会被下一次 POST 整体覆盖（现在拒写 + 500）。
- [ ] **下拉面板里没有「全部」项**（2026-09-19 无障碍普查时顺带查出来的新问题，未修）：
      静态 `<select>` 的首项基本安全（`f_provideSystemNumber` / `f_prodBatch` / `t_leadDept` /
      `t_projectType` 首项是「加载中…」或「全部」且 `value=""`；`pageSizeSelect` /
      `docPageSize` 首项是 `10 条/页` —— 分页大小本就要有值，不算误提交）。
      **但字典接口灌进来之后的选项里没有一项是「全部」**
      （`subscription.js` 的批次/部门/系统、`subscribe-model.js` 的 DICT 都是），
      只有 `t_projectType` / `t_reviewerRole` / `f_status` / `f_isSendOutside` 显式补了
      `{value:'',label:'全部'}`。于是「不限」只能靠点 ✕ 清除按钮，面板里看不到这个选项。
      **属于产品口径一致性问题**（哪些下拉该有「全部」），要逐页定一遍再动，别一刀切加。

> **共享库的写入形态已定（2026-09-19 用户确认：只跑一份代理、大家连它）**：这种部署下并发实测安全 —— 19 轮 470 次并发 POST 全 200、不丢记录、`savers` 恒对（`upsert` 是同步事务，人数是 SQL `COUNT(DISTINCT user_key)` 现算），**不需要再加文件锁**。真会出事的是「多份代理开同一个库文件」：实测 12 轮里 11 轮 `database is locked`、「每人只推一次」时只剩 33/52 条。已补 `PRAGMA busy_timeout = 2000` 缓解，结论就是别让多个进程写同一个文件（尤其网络盘，SQLite 官方警告其锁不可靠）。细节在 `shared/README.md` 的「并发与部署形态」。
- [ ] **订阅弹窗「调用方投产/变更批次」未接进请求体 —— 本轮「补齐必填校验」刻意跳过它**：
      这个下拉在表单里标了 `*`，但 `service-api.js` 组包时 `prodBatch` / `prodBatchList`
      只取 `row.prodBatch`，`form.callerBatch` 根本没参与提交。也就是说**给它加必填拦截只会白挡用户**：
      既不能改善数据质量，也不改变请求内容 —— 属于接线问题，不是校验问题，故本轮实现者主动不做，
      只在 `subscribe-model.js` 的注释与 `tests/subscribe-model.test.js` 的用例里把这个判断钉住。
      **目标**：确认它的业务语义（订阅方自己的投产批次？还是仅用于筛选关联文档 / 批次字典）。
      **需要**：① 业务口径 + 一次成功订阅的抓包，确认后端字段到底是 prodBatch、prodBatchList 还是 batchNum；
      ② 接线进 `buildSubscribeBody`；③ 补必填校验，并顺带删掉上面那条「批次刻意不拦」的用例
      （它会先失败，正好提醒别漏）。

> **取消订阅 / 订阅状态 —— 口径已定（2026-09-19），不再是待办**：
> 后端**没有**可用的订阅查询接口，「我订阅了哪些服务」只能本机维护
> （右上角那个「已订阅 (N)」是 localStorage 里的清单，来自手动添加 / 批量导入）。
> 所以取消订阅 **= 删掉本机清单里的一条标记**（典型场景：当初添加错了），
> 不发请求、服务端关系不变；`fetchSubscribeList` 保持零调用，将来真出现查询接口
> 且拿到抓包才轮到它。代价（换浏览器 / 清缓存会丢）已写进 UI：面板来源说明、
> 计数按钮 title、删除与清空的确认文案。**别再把它当成「漏了远端同步」。**
>
> 另有一条 `esc` 的复查记录：③「`esc` 兜底零转义」已确认修复（6 处兜底均为真转义，
> `tests/table-utils.test.js` 有防线用例），故删除该条，勿重复排查。

> **task 页导出的进度与取消 —— 已做（2026-09-19）**：按页拉全的循环从 `task.js` 的
> 闭包挪进 `csv-export.js` 的 `CsvExporter.fetchAllPages(fetchPage, opts)`
> （带 `onProgress` / `signal`，node 侧可测，见 `tests/csv-export.test.js`），
> 页面侧进展写在 `#resultCount`（照订阅页「加载中 x/y」那一套，按钮文案不变宽），
> 按钮导出期间改成「取消导出」，再点一次 = `AbortController.abort()`，
> 中止与故障分开提示（`aborted` 不算「导出失败」）。
> 为了让取消能传到 fetch，`api-client` 的 `request(name, body, query, opts)` 与
> `TaskApi.fetchTaskList(cond, page, size, apiOpts)` 各多了一个可选透传参数。
> **没做的部分**：仍是**串行**拉页（`EXPORT_PAGE_SIZE 500` × 最多 10 次），
> 没并发、没断点续拉；订阅页那套并发骨架在 `publish-query.js` 的 `fetchPages()`，
> 要合的话得先确认后端扛不扛得住导出的并发度。

## 🛠 待办（2026-09-15 审计后遗留）

> 这一批是「改动成本大」或「需要外部条件」的，本轮只登记不动手。

> **`tools/mock-proxy.js` + `mock-data.js` 已删除（2026-09-19 用户拍板）**：页面/测试/脚本零引用，只有 README 提到；数据来源就是离线回放（`PROXY_OFFLINE=1 node proxy.js` + `tools/har-import.js`）。文件已移到 `~/.Trash/spider-mock-proxy-*`（可恢复），README 的目录树与命令段同步去掉了。缺缓存的接口真要用时重新抓一次，比手写 mock 更准。
- [ ] **订阅页默认窗口查询体量**：不限定调用方时要拉 ~1.08 万条 / 约 28.6MB（2608/2609 批次是大头），
      已有并发 + 增量渲染 + 进度提示。**目标**：评估是否改成后端聚合/缩小默认窗口。
      **卡点**：取决于业务上「默认要看到多久的数据」。**需要**：业务口径确认后再动。

## 📝 使用约定

- 一条待办写清三件事：**目标是什么、卡在哪、需要什么**。
- 已完成直接删，不保留「已完成」小节。
- 实现细节不写在这里——放在对应文档（设计方案 / ONBOARDING / 记忆文件）里，这里只做索引。
- **不要把抓包 / 接口报文贴进本文件**：报文里含 token / Cookie / ssopSessionId，而本文件是入库文件。
  抓包统一放 `analysis/har/`（`.gitignore` 已忽略）。
