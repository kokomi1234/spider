# memory.md — 跨 Agent 共享记忆（用户直接拍板的约定都在这里）

> **这是跨 Agent 记忆的唯一入口**：任何 Agent（WorkBuddy / Qoder / Cursor / 人）接手前先读一遍。
> 它**随仓库走**（入库、随交付包发），不依赖某个 Agent 自己的记忆目录。
>
> 分工：本文件 = **长期有效的约定与铁律**（改一条要能说出为什么）；
> `AGENTS.md` = 协同流程；`CHANGELOG.md` = 锁定与交接看板；
> `.workbuddy/memory/` = WorkBuddy 自己的当日流水（不入库，别的 Agent 不依赖它）。
> 与本文件冲突的旧约定，以本文件为准。

## 0. 用户工作习惯（对所有 Agent 生效）

- 简体中文；直接给结论和关键细节，不堆客套话。需求边界清晰时**直接执行**，含糊才问、一次问清。
- 完成一段自成体系的改动（新功能/修 bug/文档）就**主动提交并推送**，不用每次问；
  提交按主题拆，中文提交信息（`feat:`/`fix:` + 摘要），不加 AI 署名。
- UI 反馈经常一句带过（"这个反了""提示没用删掉"）——**先按业务常识定位问题，再用数据/抓包验证**，
  不要拿"当初注释里是这么写的"当挡箭牌；注释错了就改注释。

## 1. 界面铁律（2026-09-21 用户拍板）

- **不写提示文字**：`title` 悬停提示、说明段落（"提示：""口径：""本机口径：…"这类）一律不写、
  已全删。理由：没用、还干扰。用户要看全文的需求用 **`aria-label`** 承担（可访问名，不弹框）。
- 保留的反馈只有三种：**空态一行**（解释列表为什么是空的）、**校验/toast**（操作失败必须说原因）、
  **必填标记**。新增 UI 前先问自己：这句话不写，用户会不会卡住？不会就不写。

## 2. 数据口径（钉死，别再翻案）

- **订阅关系响应字段**：`prodBatch` = **提供方**最新变更批次；`prodBatchList` = **调用方**投产/变更批次。
  证据：556 行 × 发布接口缓存交叉验证 99.1%。请求体 `prodBatch` = 调用方批次筛选（后端认）；
  `putBatch` = 死字段（后端忽略，筛选 2609 返回全量 550 实证）→ 提供方批次筛选只能**前端本地过滤**。
- **常用查询存储**：**服务端 SQLite 是唯一真相源**（2026-09-20 用户拍板）。localStorage 只做
  「我的」离线镜像，严禁把团队全集写回本机（回归测试锁着）。镜像未命中一律走服务端：
  保存/删除/改名直写服务端；`?saved=<id>` 深链与部门卡回填走 `getAsync`（三页都必须用，
  同步 `get(id)` 只查镜像，同事的查询会静默拿不到 → 空条件查询）。
- **不做脱敏**：内网自用，同事姓名/工号/部门码/任务单号入库不算泄露。唯一红线：
  凭证类字面值（token / Cookie / ssopSessionId / Authorization）绝不入库。
- **抓包铁律**：没有抓包不写接口代码，一个字段都不猜。要报文先搜 `analysis/har/` 与
  `publish/cache/`（`requestBody` 即真实请求体），别让用户重抓。
- **`operationType` 编码 → 名称**（2026-09-21 用户给的抓包编码表，钉死）：37 条写在
  `publish/js/ui/publish-dialog-model.js` 的 `OP_TYPES`——**唯一来源**，筛选下拉由它派生。
  旧版那 5 条（12 删除 / 43 修改 / 45 新增 / 50 CHECKIN / 52 CHECKOUT）**全错**，真名依次是
  正式版基线 / 服务发布-审核人审核通过 / 服务发布-归档 / 服务订阅-审核人审核通过 / 服务订阅-归档；
  真正叫 `CHECKOUT` / `CHECKIN` 的是 **14 / 15**。想判编码属订阅还是发布，看响应里的
  `subscribeName`：**有值 = 订阅类、为空 = 发布类**（抓包 21 条逐条吻合）。
  ⚠️ 用户总表里**没抓到编码**的名字（下线 / 发布 / 取消发布 / 订阅下线 / 批量授权 / UNCHECK /
  删除接口与文档关系 … 约 22 个）**一律不写**：未覆盖的编码原样显示数字、不进筛选下拉
  （编码填错会让筛选静默查错数据，比少一个选项更坏）。

## 3. 控件契约（searchable-select，踩过 4 次，别改回去）

- **IME 组字中（composition）绝不程序化改写 `input.value`、不跑过滤**——改了会把输入法踢出去，
  拼音变裸英文字母。`compositionend` 后补跑一次（兼容 Safari 顺序）。
- 可访问名 = 字段名，初始化时写一次（`aria-label`，兜底「搜索选择」），paint 阶段**不许改**；
  组件输入框永远非无名（冒烟「可访问名称」检查会抓）。
- 输入即搜：**先读 value 再展开**（openDropdown 会清 value）；`title` 已删，别加回来。

## 4. 流程与工具

- **分支**：`main`=稳定线（只放能跑的）；`dev`=开发线（所有 Agent 在这提交）。
  发布 = dev 全门禁 → `--ff-only` 合并 main → 打 `stable-YYYYMMDD` 标签 → 两条线+标签分别 push。
  **push 前先 `git fetch` 看 origin/main**——GitHub 上可能有 PR 合并提交，历史会和本地分叉（踩过）。
- **交付包**：外层 `/Users/a1/Desktop/spider/`（刻意不入库）。姿势固定：
  `git checkout-index` 铺树 + Python `zipfile`（**别用 `git archive --format=zip`**，macOS unzip 解中文名会炸）
  + `git bundle --all` + `.workbuddy` tar。包必须在解压副本里真跑门禁才算数。
- **Bash 坑**：`grep` 一律加 `-E`；测本地端口加 `--noproxy '*'`；zsh 下没匹配的 glob 直接报错退出。
- **验证 UI 必须真点真打字**（playwright 探针），调 API 的探针测不出交互 bug——四个真 bug 全是真页面抓的。
- 门禁 = `node tests/run.js` 全过 + `node tests/smoke-browser.js` ALL PASS，**以实跑输出为准**；
  冒烟 FAIL 时绝不能把验证和提交塞进同一条 `&&` 链（真放过过一个 FAIL）。
- 冒烟/门禁失败先修再提交；链式命令里验证步骤失败要中断整个链。

## 5. 其他长期约定

- 完整的踩坑细节与「为什么」在 `.workbuddy/memory/MEMORY.md` 与 `归档-*.md`（WorkBuddy 本机维护）；
  与本文件重复的，以本文件为准。发现冲突 → 写进 `CHANGELOG.md` 待评审。
- 弹窗/样式铁律见 `AGENTS.md` §8 与 `theme.css`（弹窗走 class + theme.css，不写内联 ID 选择器）。
- 修改本文件 = 用户直接拍板的约定变更，**要在 `CHANGELOG.md` 留一条交接记录**。
