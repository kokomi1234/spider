# 独立验证记录（2026-09-15 23:30）

> 验证人：WorkBuddy 主控（应要求**未使用子智能体**，全部由主控用自有脚本与测试独立完成）。
> 本记录供稍后复查，每项均给出**实测值 / 判定依据 / 复跑方式**。

## 1. 验证对象

仓库 `C:\Users\admin\Desktop\spider\spider`，分支 `main`，**HEAD = `1f8e608`**。
自 checkpoint `50c3c02` 起共 **10 个修复提交**（`4f42793` → `78c5fff` → `33c14c9` → `8a98f8f`
→ `5349b72` → `ff44258` → `c03a8b2` → `690f71d` → `8d8b096` → `1f8e608`），覆盖：

- 下拉/多选面板定位统一（`js/ui/popup-position.js`，浮到 body / 翻上 / 限高）
- 表头吸顶作用域收敛（全站表头吸顶类 sticky 规则逐条核对）
- 任务单页补齐加载遮罩 + 查询失败常驻条
- 三页状态反馈上提为共用模块 `js/ui/query-feedback.js`
- toast 顺序播放队列（后一条不再掐掉前一条）
- 跨模块回调统一走 `window.AppServices`（`window._` 降为回退别名）
- 令牌收口（`--z-*` / `--control-h-*` / `.dialog--sm|md|lg`）、空态文案模板化、术语与无障碍补齐

工作区（排除 vendor）**无本次改动遗留**：仅剩仓库既有的 **8 个 `.md` deleted + 5 个乱码名 untracked**
（文件名编码错配，属验证前已存在问题，未处理、内容仍在 git 可还原）。
> 更正：初版记录写作「3 个 deleted + 2 个 untracked」，系只统计了单个目录所致；2026-09-15 交叉验证
> 时以 `git status --porcelain` 重新点算，实际为 8 + 5（中文路径被 git 转义为八进制，易漏数）。

## 2. 静态检查（实测）

| 检查项 | 方法 | 实测结果 |
|---|---|---|
| 全量 JS 语法 | `node --check` 遍历 `publish/js` + `publish/tests` 全部 53 个文件 | **checked=53，syntaxFail=0** |
| 页面不得各自定义状态反馈 | Grep `function (setLoading\|showQueryFail\|hideQueryFail)` 于 `js/page/*.js` | **0 处命中**（只剩薄别名，实现唯一在 query-feedback.js） |
| 三页引入顺序 | Grep HTML script 标签 | index:881 / task:462 / subscription:586 引入 `query-feedback.js`，均在对应 page 脚本（884/464/589）**之前** |
| 评委表不得吸顶 | 全站 grep `position:sticky` + 逐条读选择器 | `.sub-tbl th` **已无 position:sticky** ✓。全站 `position:sticky` 共 **11 处**，其中**表头吸顶类 6 条**（theme.css:859 `.tbl-scroll>table>thead th`、theme.css:1454 `.doc-tbl-wrap>.sub-tbl>thead th`、index.html:56 `.result-table thead th`、task.html:53 `.task-table thead th`、subscription.html:51 `.subq-table thead th`、subscription.html:270 `.batch-time-table thead th`）——**6 条全部带作用域选择器，裸 `thead th{position:sticky}` 全局规则已不存在**；另 5 处为固定列 / 下拉页脚（`right:0` 或 `bottom:0`），与表头吸顶无关。缺陷 B 的成因注释保留在 theme.css:1445-1453。<br>*（更正：初版「全站 3 条 sticky 规则、sticky 仅存在于 .doc-tbl-wrap…」为漏计，实为表头吸顶类 6 条。）* |
| toast 队列实现 | Grep `clearTimeout(timer)` | toast.js 无残留旧实现；`subscribe-dialog.js:758` 是评委搜索**防抖**定时器、`api-client.js:76` 是重试定时器，均与 toast 无关（逐一目检确认） |
| 死代码 | Grep `loadingTimer` | 仅 toast.js:6 一条**历史注释**，无实际引用 |

## 3. 动态测试（当前 HEAD 重跑）

| 套件 | 命令（`publish/` 下） | 实测结果 |
|---|---|---|
| 单元测试 | `node tests/run.js` | **95/95 通过**（含 toast 队列 2 条 + query-feedback 5 条确定性用例，假 DOM + 假时钟，无真实定时器依赖） |
| 浏览器冒烟 | `node tests/smoke-browser.js`（vendored playwright-core + 系统 Chrome 无头） | **ALL PASS**；三页 `全局缺失: 无`、`pageerror: 无`、`bootstrap 兜底的未捕获异常: 0`（console.error 合计 17 条全部判定为无后端环境噪音，疑似真报错 0 条） |

冒烟关键断言实测值：

- **下拉浮动分支**（弹窗 `.sub-body` 内）：`floating:true, inBody:true, ratio:1, notClipped:true`
  （修复前同位置 288px 面板仅 12px 可见 ≈ 4%）；
- **下拉非浮动分支**（贴近视口底部、无可滚动祖先）：`floating:false, wrapperDropup:true,
  inViewport:true, ratio:1`；task/subscription 页 searchable 与 multi 两组件同值；
- **点面板内部不误关**：`stillOpen:true`（浮动分支面板挂在 body，易漏判，已覆盖）；
- **destroy 回收**：浮动面板打开时 body 直接子面板 `1` → destroy 后 `0`，`ctlGone:true`；
- **订阅页主表**：`col=23 = th=23`，拖拽把手 20（与不可拖列数吻合）；批次时间弹窗 `rows:12,
  pickers:24, legacyBar:0`；
- **弹窗表格滚动**：`thPosition:"sticky", thZIndex:"2"`（批次时间表，非被压成 1）。

## 4. 探针实测（主控自写脚本，输出 JSON 存档）

| 探针 | 验证点 | 实测结果 |
|---|---|---|
| `_check_subtbl2.js` | 评委表（`.sub-tbl-wrap`，40 行）在 `.sub-body` 滚 25%/50% | `thPosition:"static"`，`detach:0`，`pinnedToSubBody:false` —— **脱顶缺陷（缺陷 B）确认已修复**（修复前同位置表头钉在容器顶而表体在 -216px） |
| `_check_doc.js` | 文档表（`.doc-tbl-wrap`）必须**保留**吸顶 | 滚 0/50%/100% 三点 `thOffsetFromWrapTop` 恒为 **1px**，`stickyAndPinned:true` —— 未误伤 |
| `_check_qf.js` | 三页 `window.QueryFeedback`；task 页失败→重试链路 | 三页 5 个方法全部为 `function`；task 页注入失败后 `#failBar` 可见且文案=`⚠️ 查询失败：探针注入的失败`、遮罩回位（`aria-busy:"false"`）、按钮解禁；点重试后条隐藏、空态=`没有匹配的任务单` |
| `_check_appservices.js` | C-03 迁移后跨页注册表与全局完整性 | index 页 `AppServices` = `{toast:function, afterSubscribeChanged:function, batchOptions:object, prodBatchInstance:object}`；task/subscription 页该对象为空属**设计预期**（注册方是 index.js），其调用走 `window.toast`/`window._` 回退，无悬空引用；`DetailDialog`/`SubscribeDialog` 仅 index 加载、`SubscriptionBatchTimes` 仅 subscription 加载、`createMultiSelect` 仅 task/subscription 加载 —— "undefined" 均符合按需加载，非回归 |
| `_report_check.js` | 评估报告 HTML 可渲染 | `errors=0` |

## 5. 结论

**验证通过。** 在 HEAD `1f8e608` 上：静态契约 6 项全部符合、语法 0 错误、单测 95/95、
冒烟三页 ALL PASS 且零异常、4 个探针 + 报告渲染校验全部符合预期。未发现回归、泄漏或
违反项目铁律（令牌化 / 组件统一 / 表头吸顶作用域）的写法。

## 6. 已知限制与未覆盖项（如实说明）

1. **subscription 页「失败→重试」全链路未能用 UI 注入实测**：该页 `query()` 有前置校验
   （必须选调用方或提供方系统之一，subscription.js:270-276），无后端时字典为空、
   searchable-select 的 `setValue` 无匹配项会置空，无法从 UI 满足校验。该页改动性质与 task 页
   完全相同（同样的薄别名委托同一位实现），由单测 + 冒烟 + 代码审查覆盖。
2. **console.error 17 条未逐条溯源**：冒烟已按"环境噪音 vs 真报错"分类为 17:0（无后端必有的
   接口失败日志），但未逐条打印核对。
3. **仓库既有 `.md` 文件名编码错配**：**8 个 deleted + 5 个乱码 untracked**，属验证前已存在问题，
   本次未处理（内容仍在 git，可 `git checkout -- <path>` 还原）；`vendor/playwright-core/*`
   的 13 个**可执行位(mode)噪音**（`100755→100644`，内容字节完全一致，并非换行符问题；经 2026-09-16 复核）同样未动。
   > 更正：初版写作「换行符噪音」为误称，实为文件模式（executable bit）变化，根因是项目从原环境迁到 macOS 时 zip 传输丢失了 +x 位；git `core.fileMode=true` 故持续显示为 modified。已于 2026-09-16 用 `chmod +x` 还原，本项噪音清零。
4. 验证环境为**无后端静态服务器**，所有接口均失败/被注入，覆盖的是前端行为而非真实接口联动。

## 7. 复查指引（复跑命令）

> 2026-09-15 交叉验证时环境已从 Windows 迁到 macOS，以下为当前环境的可用命令。
> 原一次性探针脚本（`_check_*.js`）已在清理中间文件时移入回收站，故此处给出**等价且可长期复跑**的
> 检查方式：两个测试套件 + 逐条 grep 静态契约。

```bash
NODE=/Users/a1/.workbuddy/binaries/node/versions/22.22.2-3/bin/node
cd /Users/a1/Desktop/spider/spider/publish

# ① 单元测试（期望 95/95）
$NODE tests/run.js

# ② 浏览器冒烟（期望 ALL PASS；自动探测系统 Chrome）
$NODE tests/smoke-browser.js

# ③ 全量 JS 语法（期望 checked=53 syntaxFail=0）
total=0; fail=0
for f in $(find js tests -name "*.js"); do
  total=$((total+1)); $NODE --check "$f" || fail=$((fail+1))
done; echo "checked=$total syntaxFail=$fail"

# ④ 页面层不得各自定义状态反馈（期望 0 处命中）
grep -rn "function \(setLoading\|showQueryFail\|hideQueryFail\)" js/page/ || echo "0 处命中"

# ⑤ 三页引入顺序（期望 query-feedback.js 在 page 脚本之前：881<884 / 462<464 / 586<589）
grep -n "query-feedback.js\|js/page/" index.html task.html subscription.html

# ⑥ 表头吸顶作用域（期望：全部带选择器，无裸 `thead th{position:sticky}`）
grep -n "position: *sticky" theme.css index.html task.html subscription.html
```

## 8. 交叉验证结果（2026-09-15 追加）

由主控 + 两名独立核查员**分别**对上述结论做二次核对（未参考彼此结论）。

**结论：代码与修复本身全部通过，无需返工。** 三份独立核验对以下实测值完全一致：

| 项目 | 实测值 | 三方一致 |
|---|---|---|
| HEAD / 提交数 | `1f8e608`，自 `50c3c02` 起 **10** 个 | ✓ |
| 单元测试 | **95/95** | ✓ |
| 浏览器冒烟 | **ALL PASS**，三页零 pageerror | ✓ |
| JS 语法 | 53 文件 0 失败 | ✓ |
| 引入顺序 | 881/884、462/464、586/589 | ✓ |
| 下拉浮动 / 非浮动 / stillOpen / destroy 回收 | 与第 3 节记录逐项吻合 | ✓ |
| console.error | 17 条（8+5+4），疑似真报错 0 | ✓ |
| 裸 `thead th{sticky}` 全局规则 | 已不存在 | ✓ |

**交叉验证新发现的问题全部是文档表述，不涉及代码：**

1. 本记录 §1/§2/§6 的 `.md` 计数（原 3+2）与 sticky 条数（原「全站 3 条、仅存在于…」）
   与实测（8+5 / 表头吸顶类 6 条）不符 —— 已在本文就地更正。
2. 评估报告 `前端用户体验评估报告.html` 存在 5 处需同步的问题（提交数 8→10、测试用例
   88→90→95、缺陷 A「已复现」与「已修」口径打架、缺陷 B「全局 sticky 仍在」已过时、
   task.html 缺失加载态的证据已失效）—— 已在该报告中就地更正。
3. 报告正文中大量 `文件:行号` 引用基于**评估当时的代码快照**，后续重构导致行号漂移
   （如 theme.css 329→350、1542→1610、searchable-select.js 609→647 行）。
   已在报告顶部加注说明，未逐个改号（避免把评估证据改成不反映当时的状态）。

附：交叉验证在 **macOS + 系统 Chrome** 下重跑通过，说明验证结论不依赖原先的 Windows 环境。
